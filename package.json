{
  "name": "shapez-vortex",
  "version": "0.14.0",
  "private": true,
  "scripts": {
    "dev": "bun run --filter shapez-vortex-sveltekit --filter shapez-vortex-pocketbase dev",
    "check": "bun run --filter '*' check",
    "test": "bun run --filter '*' test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
