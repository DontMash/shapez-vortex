include:
  - caddy-volumes.compose.yml
  - pocketbase-volumes.compose.yml

name: shapez-vortex
services:
  proxy:
    extends:
      file: caddy.compose.yml
      service: caddy
  db:
    extends:
      file: pocketbase.compose.yml
      service: pocketbase
